{
  "name": "gurnard-beach-walk",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test tests/e2e/beach-picker.spec.js --reporter=list",
    "check": "node --check app.js && node --check beach-core.js && node --check playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
